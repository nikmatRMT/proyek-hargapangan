# Example environment variables for harga-backend
# COPY this file to `.env` and fill with real values for local testing.
# DO NOT commit your real `.env` to git. Use Vercel/secret manager for production.

# MongoDB connection string (Atlas SRV or standard URI)
# Example: mongodb+srv://<user>:<pass>@cluster0.mongodb.net/harpa?retryWrites=true&w=majority
MONGO_URI=

# Optional alias that some libraries expect
MONGODB_URI=

# Express session secret (use long random value)
SESSION_SECRET=

# Origin for frontend (used by CORS and cookie sameSite checks)
FRONTEND_ORIGIN=

# Node environment
NODE_ENV=production

# Port for local development (Vercel/production sets PORT automatically)
PORT=4000

# Notes:
# - Do not commit this file with real secrets.
# - On Vercel, add the same keys in Project Settings -> Environment Variables.
# - If you rotate credentials, update the value in Vercel and redeploy.
